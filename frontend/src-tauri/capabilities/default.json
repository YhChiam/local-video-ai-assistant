{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Unlock local desktop file system browsing operations",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:default",
    "shell:allow-open"
  ]
}